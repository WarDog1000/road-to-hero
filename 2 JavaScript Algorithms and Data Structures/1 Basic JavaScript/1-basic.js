// comnetario en una linea
/* comentario
en varias lineas
*/

// Tipos de datos 
var miVariable = 'frCodeCamp';
miVariable = 16;
miVariable = false;
console.log(miVariable)

let miNombre = 'Jhony';
miNombre;

var a;
var b = 2;

a
b
a = 1; // operador de asignacion =
a

// Asignar el valor de una variable a otra variable
a = 5;
b = a;
a
b

// Visualizar variables
var x; // declarar variable
x = 9; // inicializar variable
var camelCase = 'Nomenclatura standart por convencion en javaScript';

// Variables no inicializadas
var c;
c
c = a;
c

// Mayusculas y Minusculas 'Case Censitive'
var miVar = 5;
// console.log(mivar); // error: mivar is not defined

// Operaciones Aritmeticas
a
b

var suma = a + b;
suma

var resta = a - b;
resta

var producto = a * b;
producto

var cociente = a / b;
cociente

cociente = a / 0;
cociente

var resto = 10 % 2;
resto

// Numeros decimales 'float'
a = miNumeroDecimal = 23.4;
b = peso = 78.34;

producto = a * b;
producto

cociente = a / b;
cociente

cociente = a / 0;
cociente

// Incrementar el valor de una variable
var librosComprados = 105;
librosComprados

librosComprados = librosComprados + 1;
librosComprados

librosComprados += 1;
librosComprados

librosComprados++;
librosComprados

// Reducir el valor de una variable
var numeroEstudiantes = 256;
numeroEstudiantes

numeroEstudiantes = numeroEstudiantes - 1;
numeroEstudiantes

numeroEstudiantes -= 1;
numeroEstudiantes

numeroEstudiantes--;
numeroEstudiantes

// Asignacion de suma
a = 23;
a += 1
a

// Asignacion de resta
a = 23;
a -= 1;
a

// Asignacion de multiplicacion
a = 23;
a *= 2;
a

// Asignacion de divicion
a = 39;
a /= 3;
a





